import { parseTotalFromContentRange } from '../helpers/content-range.js';
class AlertChannels {
    api;
    constructor(api) {
        this.api = api;
    }
    async getAllPaginated(params = {}) {
        const response = await this.api.get('/v1/alert-channels', { params });
        const total = parseTotalFromContentRange(response.headers['content-range']) ?? response.data.length;
        return {
            alertChannels: response.data,
            total,
            page: params.page ?? 1,
            limit: params.limit ?? 10,
        };
    }
    async get(id) {
        const response = await this.api.get(`/v1/alert-channels/${id}`);
        return response.data;
    }
}
export default AlertChannels;
//# sourceMappingURL=alert-channels.js.map