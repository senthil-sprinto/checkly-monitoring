import { parseTotalFromContentRange } from '../helpers/content-range.js';
class AlertNotifications {
    api;
    constructor(api) {
        this.api = api;
    }
    async getAll(params = {}) {
        const response = await this.api.get('/v1/alert-notifications', { params });
        const total = parseTotalFromContentRange(response.headers['content-range']) ?? response.data.length;
        return {
            data: response.data,
            total,
            page: params.page ?? 1,
            limit: params.limit ?? 10,
        };
    }
}
export default AlertNotifications;
//# sourceMappingURL=alert-notifications.js.map