export interface EngineResolution {
    version: string;
    notices: string[];
    denied: boolean;
}
export declare function resolveEngineVersion(version: string, engineName: string): Promise<EngineResolution>;
