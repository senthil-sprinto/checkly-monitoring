import { RetryStrategy } from '../constructs/index.js';
import AbstractCheckRunner, { RunLocation, SequenceId } from './abstract-check-runner.js';
import { GitInformation } from './util.js';
export default class TriggerRunner extends AbstractCheckRunner {
    shouldRecord: boolean;
    location: RunLocation;
    targetTags: string[][];
    targetCheckIds: string[];
    envVars: Array<{
        key: string;
        value: string;
    }>;
    repoInfo: GitInformation | null;
    environment: string | null;
    testSessionName: string | undefined;
    testRetryStrategy: RetryStrategy | null;
    refreshCache: boolean;
    constructor(accountId: string, timeout: number, verbose: boolean, shouldRecord: boolean, location: RunLocation, targetTags: string[][], targetCheckIds: string[], envVars: Array<{
        key: string;
        value: string;
    }>, repoInfo: GitInformation | null, environment: string | null, testSessionName: string | undefined, testRetryStrategy: RetryStrategy | null, refreshCache?: boolean, detach?: boolean);
    scheduleChecks(checkRunSuiteId: string): Promise<{
        testSessionId?: string;
        checks: Array<{
            check: any;
            sequenceId: SequenceId;
        }>;
    }>;
}
