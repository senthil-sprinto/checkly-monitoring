import AbstractCheckRunner, { RunLocation, SequenceId } from './abstract-check-runner.js';
import { GitInformation } from './util.js';
import { Check } from '../constructs/check.js';
import { RetryStrategy, SharedFile } from '../constructs/index.js';
import { ProjectBundle, ResourceDataBundle } from '../constructs/project-bundle.js';
export default class TestRunner extends AbstractCheckRunner {
    projectBundle: ProjectBundle;
    checkBundles: ResourceDataBundle<Check>[];
    sharedFiles: SharedFile[];
    location: RunLocation;
    shouldRecord: boolean;
    repoInfo: GitInformation | null;
    environment: string | null;
    updateSnapshots: boolean;
    baseDirectory: string;
    testRetryStrategy: RetryStrategy | null;
    streamLogs: boolean;
    refreshCache: boolean;
    constructor(accountId: string, projectBundle: ProjectBundle, checkBundles: ResourceDataBundle<Check>[], sharedFiles: SharedFile[], location: RunLocation, timeout: number, verbose: boolean, shouldRecord: boolean, repoInfo: GitInformation | null, environment: string | null, updateSnapshots: boolean, baseDirectory: string, testRetryStrategy: RetryStrategy | null, streamLogs?: boolean, refreshCache?: boolean, detach?: boolean);
    scheduleChecks(checkRunSuiteId: string): Promise<{
        testSessionId?: string;
        checks: Array<{
            check: any;
            sequenceId: SequenceId;
        }>;
    }>;
    processCheckResult(result: any): Promise<void>;
}
