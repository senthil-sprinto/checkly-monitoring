import { Bundle } from './construct.js';
import { Dashboard } from './dashboard.js';
export interface DashboardBundleProps {
    customCSS?: string;
}
export declare class DashboardBundle implements Bundle {
    dashboard: Dashboard;
    customCSS?: string;
    constructor(dashboard: Dashboard, props: DashboardBundleProps);
    synthesize(): any;
}
